temp_c = float(input("Enter temperature in Celsius: "))
temp_f = (temp_c * 9/5) + 32
print("Temperature in Fahrenheit:", temp_f)
print("That's a difference of", temp_f - temp_c, "degrees between scales.")
